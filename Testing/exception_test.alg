/**
 * exception_test.alg - Creates a couple of deliberate exceptions to see if
 * popup window and termination are handled correctly
 */
algorithm {
    Node node = null;
    beginStep();
    highlight(node);
    Edge edge = null;
    highlight(edge);
    endStep();
}

//  [Last modified: 2017 01 24 at 17:40:46 GMT]
