/**
 * test_exception.alg - Creates a couple of deliberate exceptions to see if
 * popup window and termination are handled correctly
 */
algorithm {
    Node node = null;
    beginStep();
    highlight(node);
    Edge edge = null;
    highlight(edge);
    endStep();
}

//  [Last modified: 2016 12 13 at 20:50:21 GMT]
